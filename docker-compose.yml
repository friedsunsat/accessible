version: "3"

services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    environment:
      DATA_ROOT: ${DATA_ROOT:-/app/data}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
